import React from 'react';
import ReactDOM from 'react-dom';

const App = () => <h1>React Typescript Template</h1>;

const root = document.createElement('div');
document.body.append(root);
ReactDOM.render(<App />, root);
